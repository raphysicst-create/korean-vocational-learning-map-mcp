# Changelog

## 0.4.0 — 2026-08-25

수록 데이터는 바뀌지 않았다 (528과목 · 성취기준 47,625건 · 원문 100% · 17계열 + 전문공통).

### 권리 고지

- 성취기준 원문의 근거를 저작권법 **제24조의2(공공저작물)에서 제7조 제2호(고시는 보호대상 제외)로 교체**했다. 고시 본문이 전문 교과 교육과정을 "별책23~39와 같다"라고 규정해 별책을 고시의 구성 부분으로 편입한다.
- `RIGHTS.md` 신설 — 별책17권의 고시 편입 근거·공식 URL·SHA-256·배포 판정. 제24조의2는 예비적 검토 사항으로만 병기한다.
- `pipeline/sources.json`에 **별책23~39의 URL·SHA-256을 직접 고정**했다. 이전에는 상류 `shared/source-manifest.json`에서 읽어 간접적으로만 고정돼 있었다. `fetch`는 상류와 대조해 어긋나면 조용히 따라가지 않고 실패한다.
- `verify`에 **RIGHTS.md ↔ sources.json 전수 대조 게이트**를 추가했다 (별책번호·URL·해시·전문교과명·양방향 누락).
- about 리소스·README에 수록 원문 기준 고시(국가교육위원회 고시 제2024-3호)를 명시했다.

### 성능

- `core/routing-index.json` 추가 — 주제·클러스터·성취기준 코드에서 계열로 가는 라우팅 색인. ID·코드 조회가 전 계열(117MB)을 로드하던 것을 해당 계열만 열도록 바꿨다 (전체 로드 2.0초 → 기동 0.28초). `verify`가 색인과 실데이터를 전수 대조한다.

### 도구

- `list_clusters`에 `offset`을 추가했다. `limit` 상한(50) 너머의 구간에 도달할 수 있고, 응답에 `offset` 필드가 실린다. 필터 없는 목록은 색인에서 해당 페이지 구간만 잘라 그 페이지가 속한 계열만 로드한다.
- **버그 수정** — 미지 코드·ID의 유사 후보를 라우팅 색인 앞 2,000건에서만 찾아, 색인 뒷부분 계열의 오타에 무관한 계열의 후보를 제안했다. 앞 2,000건이 18계열 중 `agriculture-livestock` 하나만 덮었다 (실측: `[음료06-06-059]` → 농림·축산 코드 3건).
- **버그 수정** — 불투명 해시 ID(주제·클러스터)에 길이 비례 임계값(약 14)이 적용돼, 존재하지 않는 ID에도 무관한 ID를 "유사 후보"로 지어냈다. 해시 ID 조회에만 편집거리 상한 3을 건다. 성취기준 코드·과목명 제안은 그대로다.

### 개발

- GitHub Actions CI 신설 — Node 20·22·24에서 테스트·전수 검증·패키지 빌드, MCP 레지스트리 메타데이터(`server.json`) 검증. `npm audit`은 별도 잡에서 `--audit-level=high`로 1회만 돈다.
- `pipeline:verify:check` — manifest를 다시 쓰지 않는 검증 모드 (CI용).

## 0.3.0 — 2026-08-11

**파괴적 변경 — 응답 스키마에서 단계 출시 잔재 필드 제거.** 전 범위 수록으로 값이
상수가 되어 소비자를 오도하던 필드다 (실측: `included` 528건 전부 true,
`includedCategories` 18계열 전부 동일, `dataAvailable` 항상 true).

- `list_major_fields` — `includedCategories`·`dataAvailable` 제거
- `list_curricula` — 계열 요약의 `includedCourseCount`, 과목 레코드의 `included` 제거
- `ensureScope`의 사문 조건 `c.included &&` 제거
- 설계 스펙에 "현행과 달라진 지점" 후기 절 추가 (본문은 이력으로 보존)

데이터 파일과 파이프라인은 변경하지 않았다 — 위 필드는 계속 생성되며 서버가 응답에
싣지 않을 뿐이다.

## 0.2.0 — 2026-08-11

- 전공실무 309과목(성취기준 39,200건) 수록 — 전문교과 전 범위 완성 (528과목·47,625건·원문 100%)
- get_standard에 majorField 파라미터 추가 (계열 지정 조회)
- data-store: 원문 수 검사를 병합 전으로 이동(부분 병합 잔류 제거), 사어 인덱스 제거
- 추출 컷 패턴·listItem 판정 회귀 테스트 추가
- 추출 컷 수정: PDF가 괄호구를 줄바꿈으로 갈라 닫는 괄호가 줄 첫머리에 온 경우를 헤딩으로
  오인해 본문이 단어 중간에서 잘리던 문제(실측 3건 — [선통 07-02-01]·[컴생 07-02-04]·[패소 05-01-02])
- 코드 행 컷도 대시 이형(‐‑–—−)을 허용해 codePattern과 정책 일치
- verify: 원문이 요약문의 진부분 접두사인 절단 의심 건을 전수 게이트로 추가
- list_clusters에 limit 추가 — 무필터 목록이 4,480건 전량을 반환하던 문제(기본 20·최대 50)

## 0.1.0 — 2026-08-09

- 최초 공개: 전공일반 216 + 전문공통 3 = 219과목, 성취기준 8,425건(공식 원문 전량),
  18계열 분할 + 지연 로딩, MCP 도구 10종
